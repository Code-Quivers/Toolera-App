import { HomePageClient } from "@/components/cms/HomePageClient";

export default function HomePage() {
  return (
    <div className="flex flex-col min-h-screen">
      <HomePageClient />
    </div>
  );
}
